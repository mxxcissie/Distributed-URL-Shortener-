# URL Shortener

A production-style backend URL shortener built with FastAPI, PostgreSQL, Redis, Docker, and GitHub Actions.

## Status
- Project initialized
- FastAPI app created
- Base folder structure created

## Run locally

In terminal, run:

    uvicorn app.main:app --reload

## Health check

Open in browser:

    http://127.0.0.1:8000/health
